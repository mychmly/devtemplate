# Agent 公共入口

这是带最小机械队列的项目模板。用户正常下达指令，Agent 负责代书，队列工具维护状态与历史；不要要求用户编辑机器账或重复批准已经明确授权的范围。

## 开始前

1. 读 [协作边界](charter/AGENTS.md)、[项目接入信息](charter/project.md) 与 [项目目标](truth/goals.md)，确认批准人、项目位置和可写范围。接入信息不完整时先协助填写与澄清，不猜权限。
2. 读 [任务规则](queue/AGENTS.md) 与 [队列工具用法](tool/queue-usage.md)。运行 `python3 tool/task_queue.py doctor`；未初始化时按用法接入，不把“保护未就绪”当作可以无保护施工。
3. 通过工具查询任务状态，读取对应任务 Markdown、批准基线与证据。没有任务时起草方案并登记；用户已明确授权实施的，可在登记时同时记录授权，不另问一次形式上的批准。
4. 简短汇报本轮任务、实际读了什么、缺失或冲突与下一步。触及某区前读该区 AGENTS.md；对象另遵守自身规则。

## 执行与收尾

- 任务机器账在 `.shell/queue/ledger.jsonl`；[queue/tasks](queue/tasks/) 下的任务、批准基线与回执由工具维护。不得直接改账、改生成视图或手动放入附件。
- 新建、修订、批准、领取、交付、交接与收官都经队列工具。每次逻辑操作使用稳定请求号；重试沿用该号，先看工具返回的结果，不盲目再造一笔。
- 认领与状态版本以工具为准。多个 Agent 可以调用同一主工作树的队列；对象文件的并行施工不由本工具隔离，仍须协调。
- 验证事实与用户验收分开。用户要求“去做”不是“已经通过”；通过记录必须有用户指令依据，但不要求额外签名或批准界面。
- 收尾经工具记录交付或交接。同批暂存机器账、任务视图和相关工件，让提交检查核对实际暂存内容；不绕过钩子。

人类使用者从 [README](README.md) 开始。上级约束和用户当前明确指令优先；发现冲突时说明，不用模板扩大授权。
